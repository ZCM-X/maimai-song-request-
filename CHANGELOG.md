# Changelog

## v1.0.7

针对「点歌 / 跳转时游戏卡一下」的优化。先说结论（都是本机实测，毫秒）：

| | 优化前 | 优化后 |
|---|---|---|
| 曲库 JSON 重建（热） | 29 ~ 76 ms | **3.9 ms** |
| 首次冷导出 | 29 ms | 33 ms（不变，只发生在标题画面） |
| 选曲界面里每 5 秒一次的整表重探 | 每次都做 | **不做了**（改便宜的计数信号） |

- **修复：曲目表每 5 秒被整表重探一次。** `SongTable.Tick` 判断"曲库有没有变"用的是 `RawCount()` → `Snapshot(false)`，而快照 TTL 只有 800 ms，所以**每个 tick 都会重探一遍**（选曲列表 + `GetMusics()` + 反射全部 DataManager 候选字段 + 去重排序）。玩家在选曲界面待着的时候，这就是每 5 秒一次的主线程停顿。现在改成**便宜信号**：在选曲界面只数各分类条目数（微秒级），不在选曲界面拉长到 60 秒才重探一次
- **优化：每首歌的 JSON 片段缓存。** 重建曲库时原来对 1682 首逐首做反射（名字 / 流派 / 等级）和别名查表再拼 1.2 MB JSON，占 29~76 ms —— 一次 30~76 ms 的主线程停顿就是掉 2~5 帧，正好在"进选曲界面 / 点歌"那一下。现在把每首歌拆成**静态部分**（名字/艺人/流派/BPM/版本/别名/等级，只做一次）+ **动态部分**（谱面类型 STD/DX + 每档 enable/playable，用 12 位签名判断要不要重排），重建时基本都是 Append 缓存字符串。**导出的 JSON 与优化前逐字节一致**（sha256 对比过）
- **优化：跳转期间不再叠加其它主线程重活。** 新增 `SelectDriver.Busy`（点歌排队中 / 正在跳 / 等切难度画面），这期间曲目表不重探、不重出，曲绘编码也停一张都不做 —— 两件重活撞在同一帧上就是那下"卡"
- **新增 `GET /api/perf`**：各项耗时（次数 / 上次 / 最大 / 平均，毫秒），`?reset=1` 清零。目前埋点：`probe`（整表重探）、`build`（导出 JSON）、`jump` 及其分段 `jump.find / jump.bgm / jump.calc / jump.diff / jump.setup / jump.switch`、`job`（网页线程交给主线程的活）、`jacket.frame`（曲绘编码）。以后谁说卡，先看这个再改

- **优化：曲绘不再被反复重新编码。** 网页那边每行的封面缓存 key 原来是"每个自增号"，列表**每次重排都会变**，浏览器于是把可见封面全部重拉，mod 又在主线程一张张重新编码（实测 **平均 17.9 ms / 张，最大 42.6 ms**）。现在 key 只跟"曲绘代"有关：mod 只在曲目数真的变了（`Jackets.ClearCache()`）时才 `/api/status` 里的 `jacketGen` +1，平时 URL 不变、浏览器直接用缓存。点歌/切筛选/重排列表不再触发整屏重新编码
> 跳转里 `jump.bgm`（游戏自己换 BGM）和 `jump.switch`（进难度画面）是游戏自身的资源开销，我们只能保证不去叠加、不去拖长它。
## v1.0.6

- **修复：点不了 DX 谱。** 游戏里「一首歌只有一张卡」，DX / 标准谱靠 MusicSelectProcess.ScoreType 切换，而旧代码用**找到的那张卡的 id** 来判断类型（`realId >= 10000 ? DX : STD`）。点 DX 行时是靠 `id % 10000` 落到同一首歌的标准卡上，`realId` 于是是标准谱的小 id，永远算成标准 —— 所以同时有 DX+标准 谱的歌（本机 76 首，其中 75 首 DX id = 标准 id + 10000）怎么点都是标准谱
- 现在谱面类型按「用户点的那首」来定，判定顺序：网页明确指定 → 卡片里哪一套的 id 正好等于点的 id（精确命中）→ 按点的 id 的区间 → 卡上没有这种谱面就退回另一种
- `POST /api/play` 新增表单参数 `type=dx|std`（也认 `dx=1|0`）；网页点歌时会带上：这首歌只有一种就传那一种，两种都有就跟随「全部 / DX / 标准」筛选，没筛选默认 DX（网页提示与跳转结果里都会显示是 DX 还是 STD）
- 难度与「这档谱能不能玩」改成按对应类型的曲目 id 查（标准谱和 DX 谱的档位不一样，例如只有 DX 才有 Re:MASTER），避免跳 DX 时按标准谱的档位把难度压低
- 兜底：若同一首歌的 DX / 标准 id 不是差 10000（例如 Trust 695 / 11726），选曲列表里按曲名再找一遍
- 点歌后下一帧会读回游戏的 `ScoreType` 确认生效，没生效会打一条报错（默认日志级别就能看到）
## v1.0.5

稳定性 / 性能版本，主要修「点歌时游戏卡死、没有回应」。感谢 **[chidi0716](https://github.com/chidi0716)** 的报告与实现（[Issue #2](https://github.com/ZCM-X/maimai-song-request-/issues/2) / `upstream-optimizations` 分支），本版在其基础上并入并做了取舍。

- **修复（卡死主因）：实时面板推送（SSE）以前由游戏主线程直接写网络。** 只要有一个浏览器不收数据（手机锁屏 / 切后台 / 网络卡），`Write` 就会一直阻塞，游戏主线程跟着卡死，表现为「没有回应」。现在每个 SSE 连接一条独立写线程，主线程只放最新状态；单次写超过 10 秒的连接直接断开
- **修复：曲目表索引 / 别名表是普通 `Dictionary`，主线程（游戏数据变化时重建）和网页线程（查询）会同时清空重写**，可能把表写坏、查询死循环 —— 同样是卡死。现在曲目表只在主线程重建，整张新表建好后再一次性换引用；需要游戏数据的网页请求（刷新曲库 / 自检）统一交给主线程执行
- **修复：曲库 `rev` 每重建一次就 +1，网页看到 `rev` 变化又强制重建**，开着两个以上页面时互相触发，每隔几秒整表重建并清空曲绘缓存。现在内容真的变了才 +1；自动同步直接取 mod 已建好的新表，只有手动点「刷新曲库」才让 mod 重新扫描
- **修复：点歌不检查游戏状态、连点时同一帧把所有请求都执行一遍。** 现在只在选曲主流程且处于选曲列表 / 难度画面时跳转（其它画面提示先回选曲列表）；游戏锁输入（切画面动画中）先等最多 3 秒；连点只执行最新一个，前面的返回 `{"ok":false,"superseded":true}`（网页不当错误报）；切难度画面改用游戏自己的 `SyncNext`（锁定输入约 1.2 秒、所有玩家一起切、宴会场曲目进宴会场难度画面）；已在难度画面时会先正确退回选曲列表（修掉之后按返回回不去的问题）；不会跳进对战 / 挑战 / 段位 / 联机等特殊分类
- **修复：曲绘封面请求会在网页工作线程里同步等主线程编码（最多 6 秒）**，4 条网页线程被封面占满时点歌只能排队。现在封面异步返回，回写用专用后台线程；排队超过 15 秒的请求直接放弃
- **修复：配置补全缺项时会把用户改过的值全部重置成默认值**（端口、局域网访问等）。现在按当前值重写
- **修复：局域网 IP 绑定失败时整个点歌台起不来**，现在退回只开本机
- **修复：网页线程调用 Unity API**（`Application.dataPath` / `FindObjectOfType` / `Time`）、页面缓存并发写
- 优化：不在游玩中时每秒 20 次 `FindObjectOfType` 降到每秒最多 1 次；游玩中不再每 2 秒扫描曲库；别名文件每次导出只检查一次（以前每首歌查一次）；曲绘单张编码有时间预算，游玩中更省
- 网页：游戏数据加载中自动重试；SSE 连着时轮询从 180ms 降到 2 秒一次，SSE 断了 5 秒后自动重连；「难度」筛选按钮在电脑上不再被截断（统一缩写为 BAS / ADV / EXP / MAS / Re:M）；难度栏占两格
- 保留本方的取舍：**就绪门控**（游戏数据加载完之前 mod 什么都不做）与 **`崩溃保护` 默认关闭**（可选开启）

## v1.0.4

- **`崩溃保护` 改为默认关闭**：上一版默认开启的 finalizer 补丁会**吞掉游戏自身的异常**，可能让游戏"带病继续"（实测反馈：出现选曲列表为空等异常表现）。防崩不该替游戏吞异常，改为需要时手动在 toml 里开 `崩溃保护=true`
- 修复：`崩溃保护` 没被加进"配置自动补全"的键列表，老用户升级后 toml 里根本看不到这个选项；现在会自动补上
- 修正 `SongRequestMod.dll` 的程序集版本号（之前停在 1.0.2.0，导致 `/api/status` 显示的版本是旧的）
- 就绪门控保留（见 v1.0.3）

## v1.0.3

- **就绪门控**：`DataManager.IsLoaded()` 之前不再触碰任何游戏对象、也不做重活（不再每 50ms 读 `GamePlayManager.GetGameScore` / `GameManager.SelectMusicID`）。此前本 mod 从游戏第一帧就频繁访问游戏对象并叠加首帧重活（全字段反射探测 + 解析别名库 + 绑定 HTTP），会搅乱游戏自身启动阶段的多线程初始化，导致 `Process.PlInformationProcess.RestoreGhost` 空引用闪退（实测：把本 mod 移出即不崩）
- 新增可选 `崩溃保护`（给 `RestoreGhost` / `CategoryTabGenre` / `CategoryTabSort` 加 finalizer，`GetUdemaeBoss` / `GetMusicGenre` 加兜底）
## v1.0.2

- 修复：某些版本/数据下曲库显示 0 首。原因是 `DataManager` 的内部曲目表在加载完成前是**非 null 的空占位表**（`CreateDummyTable<T>()`），旧逻辑只在字段为 null 时才回退公开 API，救命路径从未被尝试；且游戏自己的 `LoadData` 会在装载阶段用 `IsDisable()` 把条目先剔掉，被剔干净后表就永远是空的。现在改为**多路探测**（选曲列表主路 + 公开 API + 反射全部候选字段 + 形状自适应抽取 + 逐条 try/catch），取条目最多的那一路；离开选曲界面后 `CombineMusicDataList` 会被游戏释放，改用最后一次成功的快照兜住，曲库不会因此掉回 0 首
- 修复：`disable` 标记在个别数据下把整库过滤为空；新增"整库被清空就自动关闭过滤"的保险，并优先用 `MusicData.disable` 字段值而不是仅看 `IsDisable()`
- 修复：`isExistsScore` 在 1.70 实际是 `List<bool>`，旧代码按 `bool[]` 读（`as bool[]`）永远是 null，导致"这档谱到底能不能玩"的游戏真值一直静默失效；现在 `bool[]` / `List<bool>` / `IList` 都认，不可玩的难度会正确置灰
- 修复：曲目表为空时会反复重建（rev 暴涨、CPU 白耗、曲绘缓存被反复清空）；现在改为节流重试（5 秒一次、最多 3 次后放弃）
- 修复：`/api/status` 的别名计数在曲库为空时永远显示 0（改为读取时即解析）
- 新增：`/api/selfcheck` 里输出曲目表数据源诊断（用了哪一路、各拿到多少条、形状、异常、disable 过滤状态），便于排查

## v1.0.1

- 曲目别名库更新：**1404 首 / 10592 条**（数据来自 MuNET 别名数据库）
- **配置文件自动补全**：启动时发现旧配置缺项（例如新增的「局域网访问」）会自动补上并重写 `SongRequestMod.toml`，保留用户已改过的值
- 启动日志去掉重复的地址行，只打一次
- 手机端顶部区域压缩（不再占掉大量屏高）、Header 不再换行堆高

## v1.0.0

- 首个公开版本
